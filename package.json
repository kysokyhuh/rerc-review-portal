{
  "name": "rerc-review-portal-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix packages/shared install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "db:generate": "npm --prefix backend exec -- prisma generate",
    "db:migrate": "npm --prefix backend exec -- prisma migrate deploy",
    "db:seed": "npm --prefix backend run seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
